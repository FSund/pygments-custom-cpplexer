int main(int argc, char *argv[]) {
    uint64_t register x;
    time_t tm;
    const double Tau = M_PI * 2;
    System header {
        vec3 p = pos(0.0, 0.0, 0.0);
        Atom *atom = Atom(p);
    }
    xyzzy is a generic keyword;
    declaration of independence;
    hello_my_name_is namespace;
    pseudo podical;
    reserved and aloof;
}
